---
all:
  children:
    master:
      hosts:
        master1:
          ansible_host: 192.168.1.10  # Replace with your master node IP
          ansible_user: ubuntu        # Replace with your SSH user
    worker:
      hosts:
        worker1:
          ansible_host: 192.168.1.11  # Replace with your worker node IP
          ansible_user: ubuntu        # Replace with your SSH user
        worker2:
          ansible_host: 192.168.1.12  # Replace with your worker node IP
          ansible_user: ubuntu        # Replace with your SSH user 